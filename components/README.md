### README — `components/`

**Purpose**

1. Composed, opinionated components
2. Encode UX decisions
3. Built on `ui` + `lib`

**Rules**

1. Can be edited per app
2. May depend on context/hooks
3. Not guaranteed reusable everywhere

**Examples**

* `UserMenu`
* `BillingPlanCard`
* `OpenInV0Button`
